plugins {
    id "com.moowork.node" version "1.2.0"
}

node {
    version = '8.7.0'
    yarnVersion = '1.2.1'
    download = true
}

yarn_test {
    environment = [CI: true]
    args = ['--', '--runInBand']
}

yarn_start.dependsOn(yarn_install)
task test(dependsOn: [yarn_install, yarn_test])
